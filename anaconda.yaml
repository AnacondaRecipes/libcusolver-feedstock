anaconda_config_version: 1
upstream_sources:
  - webpage:
      url: https://api.anaconda.org/package/conda-forge/libcusolver
      regex: '"version":\s*"([^"]+)"'
    packages:
      - libcusolver
      - libcusolver-dev
      - libcusolver-static
